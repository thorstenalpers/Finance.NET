name: "Alpha Vantage Nightly Tests"

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "main" ]
  # schedule:
    # - cron: '0 2 * * *'  # Runs nightly at 2 AM UTC

jobs:
  run-test:
    uses: ./.github/workflows/template-tests.yml
    with:
      FullyQualifiedName: "Finance.Net.Tests.IntegrationTests.AlphaVantageTests"
    secrets: inherit